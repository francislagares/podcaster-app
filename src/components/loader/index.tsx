import * as Styled from './styles';

const Loader = () => {
  return (
    <Styled.DotLoaderContainer>
      <Styled.Dot delay='200ms' />
    </Styled.DotLoaderContainer>
  );
};

export default Loader;
